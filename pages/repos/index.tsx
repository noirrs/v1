import type { FC } from "react"
import { Repo } from "@components/Design/repo"

const Repos:FC = function (){ 
    return (
        <div>
            <Repo />
        </div>
    )
   
}

export default Repos